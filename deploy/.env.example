# Domain for Caddy HTTPS (used by docker-compose.prod.yml)
DOMAIN=pilateshub.com

# Database password
DB_PASSWORD=your-secure-password-here

# App port (used by docker-compose.yml — the simple deployment without Caddy)
APP_PORT=3000

# App environment
NODE_ENV=production
