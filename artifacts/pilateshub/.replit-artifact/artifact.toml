kind = "web"
previewPath = "/"
title = "PilatesHub"
version = "1.0.0"
id = "artifacts/pilateshub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21282

[services.development]
run = "pnpm --filter @workspace/pilateshub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pilateshub", "run", "build" ]
publicDir = "artifacts/pilateshub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21282"
BASE_PATH = "/"
